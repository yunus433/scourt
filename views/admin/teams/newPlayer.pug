extends ../partials/head
block content 
  .all
    .add-players-wrapper
      h3.title Takım'a oyuncu ekle
        h5 Girdiğiniz Oyuncular
        - if (players) {
          .players-content
            each player in players 
              .personLink
                .Person
                  .numberedList
                    a.edit-player-record(href="/admin/players/edit/?id=" + player.id)
                      i.fas.fa-cog
                    a.delete-player-record(href="/admin/players/delete/?id=" + player.id onclick="return confirm('Bu oyuncu kaydını silmek istediğinize emin misiniz? Bu işlem geri alınamaz.')")
                      i.fas.fa-trash
                  .content-item.name
                    span #{player.name}
                  .content-item.position
                    span #{player.pozisyon}
                  .content-item.birthdate
                    span #{player.date}
                  .content-item.school
                    span #{player.team}
                  .content-item.gol
                    span #{player.gol}
                  .content-item.asist 
                    span #{player.asist}
        - } else {
          .player-content 
            span.no-player-info Henüz bir oyuncu eklemediniz
        - }
        h5 Yeni oyuncu girişini tamamlamak için aşağıdaki bilgileri doldurun:
        form.form(action="/admin/teams/addPlayer/?id=" + id method="POST")
          .form-line
            input(type="text" placeholder="Oyuncu İsmi" autocomplete="off" autofocus name="name")
            input(type="text" placeholder="Doğum Tarihi" autocomplete="off" name="date") 
            input(type="text" name="pozisyon" autocomplete="off" placeholder="Pozisyon")
          .form-line
            span Cinsiyet:
            select(name="gender")
              option(value="Erkek") Erkek
              option(value="Kız") Kız
            span Oyuncunun Oynadığı Ayak:
            select(name="ayak")
              option(value="Sağ") Sağ 
              option(value="Sol") Sol 
              option(value="Her ikisi de") Her ikisi de
          .form-line
            span Gol sayısı:
            input(type="number" name="gol" value=0)
            span Asist sayısı:
            input(type="number", name="asist" value=0)
          .form-line
            span Oyuncunun Uzunluğu:
            input(type="text" name="boy" autocomplete="off")
          .form-line
            span Oyuncunun Kilosu:
            input(type="text" name="kilo" autocomplete="off")
          .post-player-wrapper
            button.post-player-submit(type="submit") Oyuncuyu ekle 
        .exit-add-new-block-wrapper
          a(href="/admin/teams").exit-add-new-block Çıkış
          a(href="/admin/teams").add-new-submit Bitir  
