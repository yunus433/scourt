extends ../../partials/head
block content
  .all-wrapper
    .all-header
      a(href="/de").all-header-name scourt
    .all-content
      form.form(action="/deAuth/login" method="POST")
        input.form-inputs(type="email" name="email" placeholder="e-mail-adresse" autocomplete="off" autofocus)
        input.form-inputs(type="password" name="password" placeholder="passwort" autocomplete="off")
        - if (err) {
          - if (err == 1) { 
            .err-message 
              span.err-message-text Entschuldigung, Ihre E-Mail oder Ihr Passwort ist falsch.
          - }
        - }
        input.submit-button(type="submit" value="Anmelden")
        a.register-link(href="/deAuth/register") Haben Sie noch kein Konto?
