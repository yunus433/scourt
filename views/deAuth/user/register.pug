extends ../../partials/head
block content
  .all-wrapper
    .all-header
      a(href="/de").all-header-name scourt
    .all-content
      form.form(action="/deAuth/register" method="POST")
        input.form-inputs(type="email" name="email" placeholder="e-mail-adresse" autocomplete="off" autofocus)
        input.form-inputs(type="password" name="password" placeholder="passwort" autocomplete="off")
        input.form-inputs(type="password" name="confirmpassword" placeholder="passwort bestätigen" autocomplete="off")
        - if (err) {
          - if (err == 1) { 
            .err-message 
              span.err-message-text Bitte geben Sie alle erforderlichen Informationen ein
          - }
          - else if (err == 2) { 
            .err-message 
              span.err-message-text Bitte bestätigen Sie Ihr Passwort
          - }
          - else if (err == 3) { 
            .err-message 
              span.err-message-text Ihre E-Mail-Adresse ist nicht gültig
          - }
          - else if (err == 4) { 
            .err-message 
              span.err-message-text Ihr Passwort sollte länger als 5 Zeichen sein.
          - }
          - else if (err == 5) { 
            .err-message 
              span.err-message-text Diese E-Mail-Adresse ist bereits registriert
          - }
        - }
        input.submit-button(type="submit" value="Registrieren")
        a.login-link(href="/deAuth/login") Haben Sie bereits ein Konto?
